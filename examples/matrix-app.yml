id: example_as
url: http://10.0.5.40:9000
as_token: "AS_SUPER_SECRET_TOKEN"
hs_token: "HS_SUPER_SECRET_TOKEN"

sender_localpart: examplebot

namespaces:
  users: 
    - exclusive: true
      regex: "@mesh_.*:example.com"
#    - exclusive: true
#      regex: "@?_*:example.com"

  rooms: 
    - exclusive: true
      regex: ".*"

  aliases:
    - exclusive: true
      regex: "#mesh_.*:example.com"

