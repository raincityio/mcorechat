id: example_as
url: http://127.0.0.1:9000
as_token: "AS_SUPER_SECRET_TOKEN"
hs_token: "HS_SUPER_SECRET_TOKEN"

sender_localpart: meshbot

namespaces:
  users: 
    - exclusive: true
      regex: "@mesh..*:example.com"
#    - exclusive: true
#      regex: "@?_*:example.com"

  rooms: 
    - exclusive: true
      regex: ".*"

  aliases:
    - exclusive: true
      regex: "#mesh..*:example.com"

